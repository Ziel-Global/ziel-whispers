Project Management Module
Consolidated functional and technical specification, for the Ziel Log System
Version 1.2 (draft), adds stage-level log attribution, log-triggered stage transitions, unified reassignment, and template-scoped automation rules
What changed in this version
This version closes the loop between logging hours and moving a task through its workflow automatically, with no manual status changes and no manual reassignment, for any workflow, any project, any kind of work.
Four real additions: daily_logs now records which stage the hours were logged against; submitting a log, or declaring a stage's outcome directly, is what advances a task; task_status_history now records who held the task at the moment it moved; and automation rules can now attach to a workflow template once and apply to every project using it.

Text highlighted in yellow throughout this document is new or changed since version 1.1.
Contents
Contents1
1. Purpose and scope3
2. Design principle: configured, not hardcoded4
3. Why this order4
4. User roles and access4
5. Functional requirements5
5.1 Project overview5
5.2 Task management5
5.3 Phases5
5.4 Task assignment5
5.5 Task-to-log linkage5
5.6 Burndown tracking6
5.7 Reporting and stats6
5.8 Blockers6
5.9 Task comments6
5.10 Status updates6
5.11 Client action items6
5.12 Client portal6
5.13 Portal messaging7
5.14 Configurable workflows7
5.15 Task dependencies and critical path7
5.16 Sprints and velocity7
5.17 Project roles7
5.18 Automation rules7
5.19 Kanban board8
5.20 Stage outcome and log-triggered transitions8
6. Data model changes8
6.1 Modified core tables8
6.2 Collaboration and status-history tables9
6.3 Client portal tables10
6.4 Workflow tables12
6.5 Scheduling tables13
6.6 Sprint tables13
6.7 Roles and settings tables14
6.8 Automation tables15
6.9 AI foundation table (reserved)16
7. Business logic16
7.1 Burndown calculation16
7.2 Health status16
7.3 Client share link security17
7.4 Client action item completion17
7.5 Workflow category mapping17
7.6 Critical path calculation17
7.7 Sprint velocity17
7.8 Dependency enforcement18
7.9 Automation execution model18
7.10 Reassignment on stage change18
8. New edge functions, cron jobs, and triggers19
9. Permissions matrix19
10. Roadmap: toward an AI scrum master20
11. Assumptions and open questions21
12. Risks21
13. Rollout plan22


1. Purpose and scope
This module gives the Ziel Log System full project management capability: project detail, task lists, phases, task assignment, effort tracking against daily logs, burndown, health status, a client-facing portal, configurable workflows, task dependencies with critical path scheduling, sprint-based planning with velocity tracking, project-defined roles, a configurable automation rule engine, and a read-only kanban board.
Every new table is structured so an AI scrum master can eventually read project state and act on it, without another schema change when that work begins. Section 10 lays out how.
1.1 In scope
Project overview page with health status and phase timeline
Task list with assignment, priority, estimate, due date, client visibility control, configurable workflow status, sprint assignment, and story points
Phases, containing sprints, containing tasks
Daily log entries linked to a specific task and to the workflow status the task was in at the time, not just to the task
Burndown chart based on estimated vs. logged hours
Blockers, raised against a task or a project, with an open/resolved workflow
Task comments for internal collaboration
Periodic status updates, optionally published to the client portal
Client action items: things Ziel needs from the client, tracked to completion
Reporting: utilization, hours by phase and by workflow stage, task velocity, overdue items, blocker frequency, health trend
Expanded client portal: phase, tasks, assignees, blockers, action items, status updates, portal messaging
Configurable workflows per project, with a full history of every status change and who held the task at each one
Task dependencies, with cycle prevention and a critical path calculation
Sprints, with story points, velocity, and mid-sprint scope-change tracking
Project-defined roles, with no fixed list of job titles anywhere in the system
Per-project configurable thresholds for health status and schedule calculations
An automation rule engine: trigger, conditions, actions, scoped per project or reusable across every project on a shared workflow template
Log-triggered stage transitions: submitting a log, or declaring a stage outcome directly, is what advances a task through its workflow
Automatic reassignment on every stage change, preferring whoever previously held this task at that stage, falling back to a role only when nobody has
A read-only kanban board reflecting each project's own workflow
1.2 Foundation only, not built
The AI scrum master itself: no agent, no automated drafting, no autonomous actions
Persistent client login accounts (the portal remains a share link)
Budget or cost tracking beyond hours
Automatic status transitions or reassignments driven by an agent (stage 3 or 4 of the roadmap in section 10, distinct from the rule-based automation in this document)
Velocity forecasting or sprint scope recommendations
Hard enforcement of dependency order. This is implemented as a warning, not a block; see section 7.8
Genuinely open-ended scripted automation logic. The rule engine in this document covers a defined, extensible vocabulary of triggers and actions, not an arbitrary code execution layer
2. Design principle: configured, not hardcoded
This module is meant to work the same way regardless of what kind of project or what kind of work is running through it, a content approval chain, a development pipeline, a one-week fix, a year-long build. Four things are treated as configurable tables or settings, not constants buried in application code:
Workflow statuses and their transitions are defined per project through workflow_templates (5.14), not a fixed status list
Roles are defined per project through project_roles (5.17), not a fixed set of job titles
Health and schedule thresholds live in project_settings (6.7), not as constants in the health or critical path calculations
The automation vocabulary (5.18) is a set of plain text trigger and action types paired with jsonb configuration, so adding a new trigger or action later is new code against an existing table, not a schema migration
This version extends the same principle to reassignment: which stages exist, which moves between them are legal, and who a task goes to when it enters one are all configured per workflow, never assumed from a status name like "QA" or "content review." Anywhere this document states a specific number or a specific example role, that's a default or an illustration, not a limit.
3. Why this order
Configurable workflows come before dependencies and sprints in this document because critical path and velocity calculations both need a reliable, structured answer to whether a task is actually done, independent of whatever label a given project's workflow uses for that state. Dependencies and sprints have no ordering requirement relative to each other, and can be built in either order, or in parallel, once workflows are in place. Project roles and the automation engine both depend on workflows and, for role-based actions, on project_roles existing and being populated, so they're built last.
4. User roles and access
Three access levels apply throughout this module. The manager role referenced elsewhere in the base system's Admin Features is not yet defined against the Permissions Matrix; until that's resolved, this spec treats manager as equivalent to Admin for project management actions.
The client role has materially more read access than a simple health view: phase, client-visible tasks and assignees, client-visible blockers, status updates marked visible to client, plus one write capability, marking their own action items complete. The kanban board and automation rules are internal-only; clients don't see either.
Only a task's current assignee can submit a log or declare a stage outcome against it. This isn't just a UI convenience, it's what keeps stage-level hour attribution honest: if anyone could log against any task regardless of who it's assigned to, hours logged in a stage would no longer reliably tell you who actually did that stage's work. Admin retains an override for correcting mistakes.
Role
Access
Admin
Full control: create/edit projects, phases, tasks; set estimates, due dates, and client visibility; manage workflows, dependencies, sprints, project roles, project settings, automation rules, blockers, status updates, client action items, and portal messaging; generate and revoke client share links; view all reporting; override the current-assignee restriction on logs when correcting a mistake.
Employee
View and update tasks assigned to them; log hours and declare a stage outcome only against tasks currently assigned to them; create and remove dependencies; assign story points to their own tasks; raise and resolve blockers; post task comments and status updates; view the kanban board; view projects and phases they're a member of.
Client (share link)
Read-only view of phase, client-visible tasks, assignee names, open client-visible blockers, and status updates marked visible to client. Can mark their own client action items complete. No access to internal-only tasks, comments, blockers, individual daily log entries, schedule and velocity detail, the kanban board, or automation rules.
5. Functional requirements
5.1 Project overview
A single page per project showing name, client, dates, status, current health badge, phase timeline, team members, and document links. Presentation layer over the existing projects, project_phases, and project_members tables.
5.2 Task management
Extends the existing tasks table with:
Estimated hours: effort estimate used for burndown
Due date: task-level deadline, used for overdue detection and burndown pacing
Client visible: whether this task appears in the client portal task list, default true
Status: driven by a project's configurable workflow rather than a fixed list (see 5.14)
Sprint assignment and story points: optional, for teams planning in sprints (see 5.16)
A task keeps its phase_id regardless of sprint assignment. If a task does have a sprint, that sprint's own phase must match the task's phase, so the hierarchy stays consistent: a phase contains sprints, a sprint contains tasks, but a task without a sprint still sits directly under its phase as a backlog item. Tasks not yet estimated remain fully usable; they're excluded from burndown math and flagged separately (see 7.1).
5.3 Phases
Uses the existing project_phases table unchanged. Phase progress is the percentage of linked tasks marked complete, plotted against the phase due_date. A phase now contains sprints (5.16) as well as tasks directly.
5.4 Task assignment
Uses the existing tasks.assigned_to field. What's new in this version is that this field is now rewritten automatically on every stage change (see 5.20 and 7.10), rather than only ever being set by a person. A person can still reassign a task manually at any time; automation is what removes the need to.
5.5 Task-to-log linkage
daily_logs gains two things in this version: task_id, linking hours to a specific task, and status_id, recording which workflow status the task was in at the moment those hours were logged. That second field is what makes it possible to answer "how many hours did Development actually spend on this" separately from QA's hours on the same task, even though both are logged against the same task_id. Only the task's current assignee can submit a log against it (see section 4).
5.6 Burndown tracking
Chart scoped to a project, phase, or sprint, showing an ideal line and an actual line across the date range. Calculation detail in section 7.1. Open blockers, and schedule position, are inputs into health status alongside burndown variance, detailed in 7.2.
5.7 Reporting and stats
Utilization: hours logged by team member, across projects and tasks, and by workflow stage
Hours by phase and by category
Task velocity: tasks completed per week, and sprint velocity in story points where sprints are used
Overdue and at-risk task lists
Blocker frequency and average time to resolution
Project health trend, using the snapshot history
Automation rule activity: runs, failures, and skipped executions per rule
Stage cycle time: how long a task actually spent in each status, derived from task_status_history
5.8 Blockers
A blocker can be raised against a specific task or at the project level, with a short description and a status of open or resolved. Blockers default to client visible, so a client can see what is holding up progress, but any individual blocker can be marked internal only when the underlying cause shouldn't be surfaced externally, for example a resourcing issue inside Ziel. Open blockers feed into the health status calculation in section 7.2, and a blocker on a critical-path task is treated as more urgent than one with schedule slack.
5.9 Task comments
A lightweight comment thread on each task, used for internal collaboration between team members. The author_type field (human, ai, or system) is included from the start so this table can support AI-generated and automation-generated comments without a schema change. A system-authored comment means an automation rule wrote it; an ai-authored one means an agent's judgment produced it.
5.10 Status updates
A short narrative summary posted against a project on a regular cadence, written by a team member for now. Each update can be marked visible to client, in which case it appears in the portal as a progress log. This is the mechanism an AI scrum master will eventually use to post automated updates, with the visible_to_client flag giving the team a review point before anything reaches a client.
5.11 Client action items
Items that need something from the client rather than from the team: approving a design, providing brand assets, signing off a milestone. These appear in the portal with a pending or completed status, and the client can mark an item complete through the portal. Completion is written through a dedicated edge function rather than a direct database write, since the client has no login (see 7.4).
5.12 Client portal
Accessed via a tokenized link, no login required. Shows:
Current phase and overall project status
Task counts by status, and the task list itself for tasks marked client_visible
Who is working on what: assignee names against client-visible tasks
Open blockers marked client visible, with a short description of what's holding progress
Client action items awaiting a response
Recent status updates marked visible to client
A portal messaging block for the team to highlight relevant services or next steps (see 5.13)
Assignee names and task-level detail are shown by design, gated by the client_visible flag so the team retains control over what surfaces externally. Schedule and velocity detail, critical path position, sprint scope, automation activity, and stage-level log detail are not exposed to clients; the health badge is the client-facing signal for schedule risk.
5.13 Portal messaging
A simple content block the team can set per project, shown inside the client portal. Intended as a lightweight way to surface relevant next steps or additional services for that client's engagement. Content is entirely team-authored. This is a manually curated message, not a recommendation engine.
5.14 Configurable workflows
Projects currently share one fixed set of task statuses. This module lets each project use its own workflow: its own status names, its own sort order, its own allowed transitions between them, including branching transitions and transitions that loop backward, a content stage that can move forward to production or back to a revision stage, a QA stage that can move forward to done or back to development. A status's category, to do, in progress, or done, stays fixed regardless of its label, and every calculation elsewhere in the system, burndown, health, blockers, dependencies, sprints, automations, reads from category, never from the label. That's what lets a content team, a dev team, and a sales team each run their own workflow shape on the same system without any of it being coded per team.
5.15 Task dependencies and critical path
Tasks can depend on other tasks. A dependency has a type, matching standard project scheduling conventions, and the system prevents a dependency graph from looping back on itself. A scheduled job computes the critical path, the chain of tasks with zero slack, and stores the result rather than recalculating it live. A blocker on a critical-path task is treated as more urgent than one on a task with schedule slack (see 7.2).
5.16 Sprints and velocity
A sprint belongs to a phase, not directly to a project; a phase can contain several sprints over its lifetime. Tasks can be pulled into a time-boxed sprint rather than sitting only against a phase. A task without a sprint assignment sits in the backlog at the phase level. Story points are captured alongside estimated hours. Scope changes after a sprint starts, points added or removed, are tracked separately from points completed.
5.17 Project roles
Projects reference roles through project_roles rather than any fixed list of job titles baked into the system. A role is whatever name an admin gives it, QA, backend, designer, copywriter, account lead, and project_members links a person to a project against one of those roles.
A role is only ever used as a fallback for reassignment, see 5.20, when nobody has previously held a task at a given stage.
5.18 Automation rules
An admin-configurable rule engine: a trigger starts the rule, one or more conditions must all pass for it to continue, and one or more actions run in sequence once they do.
A rule attaches to exactly one of a project or a workflow_template; a template-scoped rule applies automatically to every project using that template, so a standard handoff pattern is configured once, not rebuilt per client engagement. A project-scoped rule takes precedence over a template-scoped one when both would match the same event, so a project can still override the template default when it genuinely needs to behave differently. See 7.9 for the execution model and 7.10 for reassignment specifically.
5.19 Kanban board
A read-only board per project, phase, or sprint. Columns are that project's own workflow_statuses in their defined sort order. Cards show task title, assignee, and priority. Read-only by design: see 7.9 for why a draggable board is a bad idea once automation rules exist.
5.20 Stage outcome and log-triggered transitions
This is what removes manual status changes from the picture entirely. When someone submits a log against their current task, they can also declare that stage complete as part of that same action. If the task's current status has exactly one legal outgoing transition per workflow_transitions, that's a simple confirmation. If it has more than one, a content stage that can move to production or back to revision, a QA stage that can move to done or back to development, the person picks which one actually happened; nobody else is in a position to know that but them.
Not every stage needs hours behind it. A quick approval step might have nothing to log at all, so declaring an outcome works as a standalone action too, not only as part of a log submission.
The declared outcome is a normal status_id change on the task. task_status_history records it automatically, including who was assigned at the moment it happened (see 6.2). That status change is exactly the event automation rules listen for, and what runs next is the reassignment described in 7.10. Nothing here is manual: the person doing the work only ever declares what actually happened, the system handles where the task goes and who it goes to.
6. Data model changes
6.1 Modified core tables
tasks
Column
Type
Notes
estimated_hours
numeric(6,2)
Nullable. Effort estimate used for burndown.
due_date
date
Nullable. Used for overdue detection and burndown pacing.
client_visible
boolean, default true
Controls whether the task appears in the client portal.
status_id
uuid, references workflow_statuses.id
Replaces the previous free-text status column.
sprint_id
uuid, references sprints.id, nullable
Sprint this task is planned in, if any. Must belong to the same phase as the task.
story_points
numeric, nullable
Estimation unit for sprint velocity, additive to estimated_hours.

daily_logs
Column
Type
Notes
task_id
uuid, references tasks.id
Nullable. Links logged hours to a specific task.
status_id
uuid, references workflow_statuses.id, nullable
New. Records which workflow status the task was in when these hours were logged, enabling stage-level attribution.

projects gains workflow_template_id (uuid, references workflow_templates.id).
6.2 Collaboration and status-history tables
blockers
Column
Type
Description
id
uuid
Primary key
project_id
uuid, references projects.id
Project the blocker belongs to
task_id
uuid, references tasks.id, nullable
Task the blocker is raised against, if task-specific
description
text
What is blocking progress
status
text
open or resolved
client_visible
boolean, default true
Whether this blocker shows in the client portal
raised_by
uuid, references users.id
Who raised the blocker
raised_at
timestamptz
When it was raised
resolved_by
uuid, references users.id, nullable
Who resolved it
resolved_at
timestamptz, nullable
When it was resolved

task_comments
Column
Type
Description
id
uuid
Primary key
task_id
uuid, references tasks.id
Task the comment belongs to
author_type
text
human, ai, or system
author_id
uuid, references users.id, nullable
Null when author_type is ai or system
body
text
Comment content
created_at
timestamptz
When the comment was posted

project_status_updates
Column
Type
Description
id
uuid
Primary key
project_id
uuid, references projects.id
Project this update covers
author_type
text
human, ai, or system
author_id
uuid, references users.id, nullable
Null when author_type is ai or system
summary
text
The update content
visible_to_client
boolean, default false
Whether this update appears in the client portal
created_at
timestamptz
When the update was posted

task_status_history
Every time a task's status changes, a row is written here. Written by a database trigger on tasks.status_id, not application code, so no code path can change a status without leaving a record.
Column
Type
Description
id
uuid
Primary key
task_id
uuid, references tasks.id
Task that changed status
from_status_id
uuid, references workflow_statuses.id, nullable
Null on a task's initial status
to_status_id
uuid, references workflow_statuses.id
New status
assigned_to_at_change
uuid, references users.id, nullable
New. Who held the task at the moment this change happened, the basis for automatic reassignment (see 7.10).
changed_by_type
text
human, ai, or system
changed_by_id
uuid, references users.id, nullable
Null when changed_by_type is ai or system
changed_at
timestamptz
When the change happened
6.3 Client portal tables
project_share_links
Column
Type
Description
id
uuid
Primary key
project_id
uuid, references projects.id
Project being shared
token
text, unique
Random token embedded in the client-facing URL
passcode_hash
text, nullable
Optional passcode protection
expires_at
timestamptz, nullable
Automatic expiry
revoked
boolean, default false
Manual revocation flag
created_by
uuid, references users.id
Admin who generated the link
created_at
timestamptz
Creation timestamp
last_viewed_at
timestamptz, nullable
Most recent client view

project_share_views
Lightweight, unauthenticated view log so the team can see whether a client has actually opened the link.
Column
Type
Description
id
uuid
Primary key
share_link_id
uuid, references project_share_links.id
Link that was viewed
viewed_at
timestamptz
Timestamp of the view

client_action_items
Column
Type
Description
id
uuid
Primary key
project_id
uuid, references projects.id
Project this item belongs to
title
text
Short title of what's needed
description
text
Detail on what's being requested
status
text
pending, completed, or waived
requested_by
uuid, references users.id
Team member who requested it
due_date
date, nullable
When it's needed by
completed_at
timestamptz, nullable
When the client marked it complete

client_portal_messages
Column
Type
Description
id
uuid
Primary key
project_id
uuid, references projects.id
Project this message is shown on
title
text
Message heading
body
text
Message content
cta_label
text, nullable
Button or link label, if any
cta_url
text, nullable
Destination for the call to action
active
boolean, default true
Whether the message currently shows
created_by
uuid, references users.id
Who wrote it
created_at
timestamptz
When it was created

project_health_snapshots
Populated by a daily cron job so health status and burndown can be charted over time.
Column
Type
Description
id
uuid
Primary key
project_id
uuid, references projects.id
Project this snapshot covers
snapshot_date
date
Date the snapshot was taken
health_status
text
on_track, at_risk, or delayed
planned_hours
numeric
Sum of estimated_hours across in-scope tasks
logged_hours
numeric
Sum of hours logged against those tasks
tasks_total
integer
Total tasks in scope
tasks_complete
integer
Tasks marked complete
tasks_overdue
integer
Tasks past due_date and not complete
open_blockers
integer
Open blockers at time of snapshot
6.4 Workflow tables
workflow_templates
Column
Type
Description
id
uuid
Primary key
name
text
Template name, for example “Content approval” or “Standard delivery”
description
text, nullable
What this template is for
created_by
uuid, references users.id
Who created it
created_at
timestamptz
Creation timestamp

workflow_statuses
Column
Type
Description
id
uuid
Primary key
workflow_template_id
uuid, references workflow_templates.id
Template this status belongs to
name
text
Status label, project-defined
category
text
todo, in_progress, or done. Drives every downstream calculation.
sort_order
integer
Display order, also the kanban column order
is_initial
boolean, default false
Default status for a newly created task

workflow_transitions
Column
Type
Description
id
uuid
Primary key
workflow_template_id
uuid, references workflow_templates.id
Template this transition belongs to
from_status_id
uuid, references workflow_statuses.id, nullable
Null means allowed from any status
to_status_id
uuid, references workflow_statuses.id
Destination status

Templates are reusable objects, not owned by a single project, so several projects can share a standard template, or a project can point to one built for a specific client.
A template's transitions can branch (more than one legal destination from a status) and loop backward (a later status transitioning back to an earlier one); both are ordinary rows in this table, nothing special is required to express either.
6.5 Scheduling tables
task_dependencies
Column
Type
Description
id
uuid
Primary key
task_id
uuid, references tasks.id
The dependent task
depends_on_task_id
uuid, references tasks.id
The task it depends on
dependency_type
text
finish_to_start, start_to_start, finish_to_finish, or start_to_finish
created_by
uuid, references users.id
Who created the dependency
created_at
timestamptz
Creation timestamp

A cycle check runs on insert: a task cannot depend, directly or transitively, on a task that depends on it.
task_schedule_snapshots
Column
Type
Description
id
uuid
Primary key
task_id
uuid, references tasks.id
Task this snapshot covers
snapshot_date
date
Date the calculation ran
earliest_start
date
Earliest possible start given dependencies
earliest_finish
date
Earliest possible finish
latest_start
date
Latest start without delaying the project
latest_finish
date
Latest finish without delaying the project
slack_days
integer
Latest start minus earliest start
is_critical
boolean
True when slack_days is zero
6.6 Sprint tables
sprints
Column
Type
Description
id
uuid
Primary key
phase_id
uuid, references project_phases.id
Phase this sprint belongs to. Project is derived through the phase, not stored separately.
name
text
Sprint name or number
start_date
date
Sprint start
end_date
date
Sprint end
status
text
planned, active, or completed
created_at
timestamptz
Creation timestamp

sprint_snapshots
Captured daily while a sprint is active, and at sprint close.
Column
Type
Description
id
uuid
Primary key
sprint_id
uuid, references sprints.id
Sprint this snapshot covers
snapshot_date
date
Date of the snapshot
committed_points
numeric
Points committed at sprint start
completed_points
numeric
Points marked done as of this snapshot
points_added_mid_sprint
numeric
Points added to the sprint after it started
points_removed_mid_sprint
numeric
Points removed from the sprint after it started
6.7 Roles and settings tables
project_roles
No fixed list anywhere. An admin defines whatever roles a project actually needs.
Column
Type
Description
id
uuid
Primary key
project_id
uuid, references projects.id, nullable
Null means a reusable role available across projects, same pattern as workflow_templates
name
text
Role name, entirely admin-defined
created_by
uuid, references users.id
Who created it
created_at
timestamptz
Creation timestamp

project_members gains role_id (uuid, references project_roles.id, nullable).
project_settings
One row per project. Every threshold used in health and schedule calculations elsewhere in this document lives here, with sensible defaults, not as a constant in the calculation itself.
Column
Type
Description
id
uuid
Primary key
project_id
uuid, references projects.id, unique
One settings row per project
at_risk_variance_percent
numeric, default 10
Burndown variance that moves a project to at risk
delayed_variance_percent
numeric, default 25
Burndown variance that moves a project to delayed
blocker_warning_days
integer, default 5
Days an open blocker sits before it counts toward delayed
critical_blocker_warning_days
integer, default 2
Same, but for a blocker on a critical-path task
hours_per_day
numeric, default 8
Used to convert estimated_hours into calendar duration for critical path
6.8 Automation tables
automation_rules
Column
Type
Description
id
uuid
Primary key
project_id
uuid, references projects.id, nullable
Set when this rule applies to one project only
workflow_template_id
uuid, references workflow_templates.id, nullable
New. Set when this rule should apply to every project using this template. Exactly one of project_id or workflow_template_id must be set, never both, never neither.
name
text
Rule name
description
text
Why this rule exists, required, not optional documentation
status
text
draft, enabled, or disabled
trigger_type
text
Plain text, not a constrained enum, see 5.18
conditions
jsonb
Array of checks, all must pass
actions
jsonb
Array of actions, run in sequence
allow_triggering_other_rules
boolean, default false
Whether this rule's actions can fire other rules
priority
integer
Resolves conflicts when multiple rules match one event; a project-scoped rule always takes precedence over a template-scoped rule regardless of priority
created_by
uuid, references users.id
Who created it
created_at
timestamptz
Creation timestamp

automation_rule_runs
Column
Type
Description
id
uuid
Primary key
automation_rule_id
uuid, references automation_rules.id
Rule that ran
entity_type
text
What kind of record triggered it, for example task
entity_id
uuid
The specific record
root_event_id
uuid
Shared across a chained sequence of rule runs, for tracing
triggered_at
timestamptz
When it fired
result
text
success, failed, skipped, or condition_not_met
error_message
text, nullable
Populated when result is failed
6.9 AI foundation table (reserved)
This table exists so an audit trail is already in place when AI-driven actions begin. It is not written to or read from anywhere in this version.
ai_agent_actions
Column
Type
Description
id
uuid
Primary key
project_id
uuid, references projects.id, nullable
Project the action relates to
action_type
text
What kind of action, defined when this is wired up
target_entity
text
Table the action would affect
target_id
uuid, nullable
Record the action would affect
payload
jsonb
Proposed change, in whatever shape the future agent produces
status
text
proposed, applied, or reverted
created_at
timestamptz
When the action was logged
7. Business logic
7.1 Burndown calculation
Remaining work for a project, phase, or sprint is the sum, across all estimated tasks in scope, of estimated_hours minus hours logged against that task, floored at zero:
remaining = SUM over tasks of max(0, task.estimated_hours - hours logged against task.id)
The ideal line runs linearly from total estimated hours down to zero, between the earliest task creation date and the phase, sprint, or project due date. The actual line is plotted from daily snapshot history. Tasks without an estimate are excluded from the calculation and shown as a separate unestimated count next to the chart, rather than blocking the feature.
7.2 Health status
Computed daily per project by a scheduled job, against that project's own project_settings row rather than one fixed threshold for every project:
Status
Condition
On track
No overdue tasks, no open blockers, and no open blocker sits on a task where is_critical is true
At risk
Burndown variance above project_settings.at_risk_variance_percent (default 10%), at least one overdue task, at least one open blocker, or any open blocker on a critical-path task regardless of its age
Delayed
Variance above project_settings.delayed_variance_percent (default 25%), a phase past due date with completion below the expected threshold, a blocker open longer than project_settings.blocker_warning_days (default 5), or a blocker on a critical-path task open longer than project_settings.critical_blocker_warning_days (default 2)

The default values above are starting points, adjustable per project through project_settings, and should be tuned once real project data is available (see section 11).
7.3 Client share link security
Token is a high-entropy random string, not a sequential or guessable ID
Optional passcode, hashed at rest, checked before rendering the client view
Configurable expiry; links default to expiring rather than staying open indefinitely
Revocable at any time from the project page, independent of expiry
A revoked or expired token returns a generic link no longer available page, not an error that reveals project existence
7.4 Client action item completion
Client action items can only be marked complete by a valid, unexpired, unrevoked share token scoped to that project. The write happens through a dedicated edge function using a service role, not a client-writable row-level security policy, since there is no client identity to scope a policy to. Completing an action item does not require anything beyond the passcode already required to view the portal, if one is set.
7.5 Workflow category mapping
Every calculation in this document, burndown, health, critical path, velocity, automation conditions, resolves a task's state through workflow_statuses.category, never through the status name. This is what lets a project's workflow use any labels it wants without breaking downstream logic.
7.6 Critical path calculation
Forward pass: for each task in dependency order, earliest_start is the latest of its dependencies' earliest_finish values (or the project start, if it has none), and earliest_finish is earliest_start plus its duration. Backward pass: starting from the project or phase target end date, latest_finish is the earliest of its dependents' latest_start values (or the target date, if it has none), and latest_start is latest_finish minus its duration. slack_days is latest_start minus earliest_start. A task is on the critical path when slack_days equals zero.
Duration for this calculation is estimated_hours divided by that project's project_settings.hours_per_day, default 8.
7.7 Sprint velocity
Velocity for a completed sprint is its completed_points. A rolling velocity figure across the last several sprints is a straightforward average once enough sprints exist; no forecasting logic is specified in this document (see section 1.2).
7.8 Dependency enforcement
This module does not hard-block a status change when a task's dependencies aren't satisfied. It surfaces a warning instead: moving a task to done, or to any in_progress status, while an unfinished finish_to_start predecessor exists shows a warning banner naming the predecessor, but does not prevent the change.
7.9 Automation execution model
Two layers do two different jobs, and the distinction matters. The workflow (workflow_statuses, workflow_transitions) is a static rulebook: it defines which stages exist for a project and which moves between them are legal. It never moves anything on its own. Automation (automation_rules) is the engine that watches for a legal move actually happening and executes real consequences, reassignment, notifications, comments, when it does. A project can have any workflow shape without any code change; automation is what makes that shape actually run itself.
A rule's trigger fires on a real event, most centrally a task's status changing (see 5.20 for how that status change gets declared in the first place), and also a blocker raised or resolved, a due date approaching, a task becoming overdue, a schedule change, a sprint milestone, or a manual or scheduled basis. Once triggered, every condition on the rule must pass, in order; if one fails, the rule stops and none of its actions run.
Actions run in the order defined on the rule, under a fixed system actor rather than impersonating whoever last touched the task, so a rule's permissions don't depend on who happened to trigger it.
A rule can trigger another rule only when allow_triggering_other_rules is explicitly set on it, and a rule can never re-trigger itself. Every chain of triggered rules shares one root_event_id in automation_rule_runs, capped at five hops.
When two enabled rules match the same event, a project-scoped rule always wins over a template-scoped one; among rules at the same scope, priority decides, and the loser is logged to automation_rule_runs as skipped.
A draggable, editable kanban board would let a casual drag-and-drop accidentally fire a reassignment rule. Keeping the board read-only (5.19) and status changes deliberate, through the declared-outcome action in 5.20, keeps every status change an intentional trigger rather than a side effect of a careless click.
7.10 Reassignment on stage change
One action type, reassign_to_stage_owner, covers every reassignment scenario in this document, forward moves and backward moves alike, with a single rule: prefer whoever previously held this exact task at this exact status, and only if nobody has, fall back to a role, resolved by balanced workload, whichever project_members holding that project_roles entry currently has the fewest tasks in a non-done status.
Concretely: the action looks up the most recent task_status_history row for this task where to_status_id matches the status just entered, and reads its assigned_to_at_change. If one exists, the task goes back to that person, no role lookup needed, this is what sends a rejected task straight back to the developer who built it rather than to whoever's least busy right now. If none exists, this is the first time this task has ever reached this status, so it falls back to the rule's configured role and balanced workload picks among the candidates. If no one currently holds that role on the project, the action fails and is logged, not silently skipped.
This means a workflow template needs exactly one reassignment rule per status that should auto-assign on entry, not two separate rules for the forward and backward case. A Development status and a QA status each get one rule; between them, every path through a task's lifecycle, first assignment, QA handoff, rejection back to the original developer, is covered automatically.
8. New edge functions, cron jobs, and triggers
Name
Trigger
Purpose
compute-project-health
pg_cron, daily
Calculate burndown variance, overdue tasks, and open blockers per active project against its own project_settings; writes a project_health_snapshots row
send-share-link
HTTP request (admin action)
Emails the client-facing project link via the existing Resend integration
validate-share-token
HTTP request (client view load)
Validates token, checks expiry, revocation, and passcode before rendering the client view
client-complete-action
HTTP request (client portal action)
Marks a client_action_items row complete after validating the share token
compute-critical-path
pg_cron, nightly
Recalculates task_schedule_snapshots for every active project, using each project's hours_per_day setting
compute-sprint-snapshot
pg_cron, daily during an active sprint
Captures committed, completed, added, and removed points
run-automation-rules
Event-driven: status change (including a declared stage outcome), blocker raised or resolved, due date reached, dependency satisfied, sprint milestone
Evaluates matching enabled rules' conditions and executes their actions, writing to automation_rule_runs
run-scheduled-automations
pg_cron, per rule's own schedule
Executes automation rules using the scheduled trigger type
A database trigger on tasks.status_id, rather than an edge function, writes task_status_history automatically whenever a task's status changes, capturing assigned_to_at_change at the same moment, so no application code path can update a task's status without leaving a complete record.
9. Permissions matrix
Feature / action
Admin
Employee
Client (share link)
Set task estimate, due date, or client visibility
Yes
No
No
Generate / revoke client share link
Yes
No
No
View project health dashboard
Yes
Assigned projects only
Own project only
View burndown chart
Yes
Assigned projects only
Aggregate view only
View client-visible task list and assignees
Yes
Assigned projects only
Yes, own project
Submit a log or declare a stage outcome against a task
Yes, any task
Current assignee only
No
Raise / resolve a blocker
Yes
Yes
No
View client-visible blockers
Yes
Yes
Yes
Post a task comment or status update
Yes
Yes
No
Mark a status update visible to client
Yes
No
No
Create / manage client action items
Yes
No
No
Mark a client action item complete
Yes
No
Yes, own project only
Manage portal messaging
Yes
No
No
Create or edit workflow templates
Yes
No
No
Assign a project's workflow template
Yes
No
No
Create or remove a task dependency
Yes
Yes
No
View critical path and schedule detail
Yes
Assigned projects only
No, health badge only
Create or manage sprints
Yes
No
No
Assign story points to a task
Yes
Yes, if assigned
No
View sprint velocity
Yes
Assigned projects only
No
Create or edit project roles
Yes
No
No
Edit project_settings thresholds
Yes
No
No
Create, edit, enable, or disable automation rules
Yes
No
No
View automation rule audit log
Yes
No
No
View the kanban board
Yes
Yes
No
10. Roadmap: toward an AI scrum master
This document lays the data foundation for an AI scrum master without building the agent itself. Every table introduced above, blockers, task_comments, project_status_updates, task_status_history, task_schedule_snapshots, sprint_snapshots, automation_rules, automation_rule_runs, and the reserved ai_agent_actions table, is structured so an agent can read project state and eventually act on it, without another schema change when that work begins.
Stage 1: Foundation (this document)
Structured data exists for blockers, comments, status updates, task status history (including who held a task at each change), schedule snapshots, sprint snapshots, and automation activity. No AI is involved. Everything is written by people, scheduled jobs, or rules a person configured.
Stage 2: Assisted drafting
An agent reads task_status_history to know what changed and who was involved, task_schedule_snapshots to know what's on the critical path, sprint_snapshots to know whether scope crept, automation_rule_runs to know what's already handled automatically, and drafts a status update or flags a likely risk. A person reviews and posts it, author_type set to ai, with a human approval step before visible_to_client is set.
Stage 3: Suggested actions
The agent proposes changes, or even proposes a new automation_rules row to handle a pattern it noticed repeating. Every proposal is written to ai_agent_actions with status proposed. A person approves or rejects before anything is applied.
Stage 4: Managed execution
Approved categories of action apply automatically, with every change still logged to ai_agent_actions and, where relevant, task_status_history or automation_rule_runs, tagged ai or system as appropriate.
No implementation decisions for stages 2 through 4 are made in this document.
11. Assumptions and open questions
Historical daily_logs rows won't have task_id or status_id; burndown, task-level, and stage-level reporting apply only from the rollout date forward
The manager role's boundary relative to Admin is undefined in the base system and needs resolving before this module's permissions can be finalized
tasks.client_visible defaults to true; the alternative, defaulting to false, is safer but adds admin overhead
project_roles needs to actually be populated per project before role-based reassignment fallback is usable. A rule referencing a role with nobody currently holding it will fail and log the failure, not silently do nothing
project_settings defaults (10%, 25%, 5 days, 2 days, 8 hours) are starting values, expected to diverge across projects once real data exists
The automation system actor bypasses normal per-user permission checks by design (7.9). If that's not acceptable, rules would need to run under a designated real user's permissions instead
The five-hop chain depth cap and the use of priority for conflict resolution are placeholders
Precedence between a project-scoped rule and a template-scoped rule is fixed as project always wins; if a project needs to fall back to the template default for some statuses and override for others, that's a per-rule decision, not a per-project one, worth confirming that's the intended granularity
A task with no prior task_status_history for a given status always falls back to the role lookup; there's no configured behavior for what happens if that role itself has nobody assigned to the project at all beyond logging the failure, worth deciding whether that should instead notify an admin directly
12. Risks
Risk
Notes
Automation misfire
Unlike a workflow label, a misconfigured rule actually reassigns work, changes a status, or messages a client. automation_rule_runs makes it diagnosable after the fact, but doesn't prevent it happening once
Estimation and scope-tracking consistency
Burndown depends on tasks being estimated in hours, velocity depends on story points, and neither works if teams fill them in inconsistently
Client portal exposure
client_visible defaulting to true means task titles and assignee names surface to clients unless actively managed
Historical data discontinuity
Task-level and stage-level burndown, and status or schedule history, has no data before rollout
Threshold tuning
Every configurable default in project_settings is a placeholder
Manager role ambiguity
An undefined permission boundary could allow or block portal, blocker, workflow, role, or automation actions incorrectly
Dependency data quality and enforcement
If teams don't consistently enter dependencies, critical path becomes meaningless for exactly the projects that need it most
Workflow and role sprawl
Without discipline, ad hoc workflow templates or project roles per client could recreate the inconsistency these features were meant to solve
Data volume and retention
task_status_history, daily schedule snapshots, and automation_rule_runs all grow continuously and need a retention plan
Silent gridlock on missing role assignment
If a stage's fallback role has nobody assigned on a project, a task can land there with no owner at all beyond a logged failure, worth an explicit notification path rather than relying on someone noticing the audit log
13. Rollout plan
Phase
Scope
1
Schema migration: task_id, estimated_hours, due_date, client_visible on tasks/daily_logs; task UI updates
2
Burndown chart and compute-project-health cron, including blocker signal
3
Blockers, task comments, and status updates for internal use
4
Client share link and expanded portal: tasks, assignees, blockers, action items, status updates, portal messaging
5
Reporting dashboard: utilization, hours by phase, velocity, overdue and blocker stats
6
Workflow templates, statuses, transitions, and task_status_history; migrate existing tasks onto a default template
7
Task dependencies, cycle prevention, the compute-critical-path job, and schedule snapshots
8
Sprints under phases, story points, the compute-sprint-snapshot job, and a velocity view
9
project_roles and project_settings; migrate current fixed thresholds into per-project defaults
10
Automation rule builder including reassign_to_stage_owner, project and template scoping, run-automation-rules, run-scheduled-automations, and the audit log
11
daily_logs.status_id and the declared-outcome action on log submission (5.20), including assigned_to_at_change on task_status_history
12
Read-only kanban board
The AI scrum master stages are intentionally excluded from this rollout plan. They follow once this module has been live long enough on real projects to give an agent something honest to read.
